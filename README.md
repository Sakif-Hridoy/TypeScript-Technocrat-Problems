# 📘TypeScript-Technocrat Blog

# 📘 Understanding Core TypeScript Concepts (Blog) -- written by Sakif with the help of AI to make designs and formats


## 1️⃣ Differences Between `interface` and `type` in TypeScript

TypeScript-এ `interface` এবং `type`—দুটোই ব্যবহার করা হয় ডেটার structure বা shape নির্ধারণ করার জন্য। দেখতে একই রকম মনে হলেও এদের ব্যবহারিক দিক থেকে কিছু মৌলিক পার্থক্য রয়েছে।

### 🔹 Interface কী এবং কেন ব্যবহার করা হয়?

`interface` মূলত object-এর structure define করার জন্য তৈরি করা হয়েছে।
এটি বিশেষভাবে উপযোগী যখন আমাদের বড় application-এ **consistent object structure** বজায় রাখতে হয়।

**Interface-এর বৈশিষ্ট্য:**

* Object-oriented programming-এর সাথে বেশি সামঞ্জস্যপূর্ণ
* একই interface একাধিকবার declare করলে TypeScript সেগুলো merge করে নেয়
* `extends` ব্যবহার করে সহজে inheritance করা যায়
* বড় project ও library design-এর ক্ষেত্রে বেশি ব্যবহৃত হয়

👉 Interface মূলত তখন ব্যবহার করা হয় যখন object-এর shape ভবিষ্যতে বাড়তে পারে।

---

### 🔹 Type কী এবং কেন ব্যবহার করা হয়?

`type` হলো একটি general-purpose type alias, যা object ছাড়াও আরও অনেক কিছু define করতে পারে।

**Type-এর বৈশিষ্ট্য:**

* Object, primitive, union, intersection—সবকিছু define করা যায়
* Complex type composition-এর জন্য বেশি flexible
* একই নামের type পুনরায় declare করা যায় না
* Intersection (`&`) ব্যবহার করে type combine করা হয়

👉 Type তখন বেশি উপযোগী যখন আমাদের flexible এবং complex type logic দরকার হয়।

---

### 🔑 লক্ষ্যনীয়

* `interface` → object structure ও scalability-এর জন্য ভালো
* `type` → flexibility ও advanced type logic-এর জন্য ভালো

---

## 2️⃣ Use of `keyof` Keyword in TypeScript

`keyof` হলো TypeScript-এর একটি powerful keyword, যা কোনো object type-এর **সব property key-এর union type** তৈরি করে।

### 🔹 `keyof` কেন গুরুত্বপূর্ণ?

Backend development-এ আমরা অনেক সময় dynamicভাবে object-এর property access করি।
`keyof` আমাদের নিশ্চিত করে যে আমরা **ভুল key ব্যবহার করছি না**।

### 🔹 `keyof` কীভাবে কাজ করে?

* একটি object type নেয়
* সেই object-এর সব key নিয়ে একটি union তৈরি করে
* শুধুমাত্র সেই key-গুলোকেই valid হিসেবে accept করে

👉 এর ফলে runtime error হওয়ার আগেই TypeScript compile-time এ error দেখায়।

---

### 🔑 ব্যবহারিক গুরুত্ব

* Type-safe dynamic programming
* Large codebase এ typo বা ভুল key access প্রতিরোধ
* Better developer experience

---

## 3️⃣ Difference Between `any`, `unknown`, and `never` Types

TypeScript-এ এই তিনটি type অনেক সময় বিভ্রান্তিকর মনে হলেও এদের উদ্দেশ্য একেবারে আলাদা।

---

### 🔴 `any` Type

`any` ব্যবহার করলে TypeScript কার্যত **type checking বন্ধ করে দেয়**।

**বৈশিষ্ট্য:**

* যেকোনো ধরনের value assign করা যায়
* যেকোনো operation করা যায়
* সবচেয়ে কম safe

👉 `any` ব্যবহার করলে TypeScript ব্যবহার করার মূল সুবিধাটাই নষ্ট হয়ে যায়।

---

### 🟡 `unknown` Type

`unknown` হলো `any`-এর safer alternative।

**বৈশিষ্ট্য:**

* যেকোনো value assign করা যায়
* ব্যবহার করার আগে type check করা বাধ্যতামূলক
* Runtime error কমায়

👉 Production-level backend code-এ `unknown` অনেক বেশি recommended।

---

### ⚫ `never` Type

`never` এমন একটি type যা বোঝায়—এই জায়গায় **কখনোই কোনো value আসবে না**।

**কখন ব্যবহার হয়?**

* Error throw করা function
* Infinite loop
* Impossible code path বোঝাতে

👉 এটি advanced type safety নিশ্চিত করতে সাহায্য করে।

---

### 🔑 তুলনামূলকভাবে

* `any` → unsafe
* `unknown` → safe but strict
* `never` → unreachable state

---

## 4️⃣ Use of Enums in TypeScript

`enum` ব্যবহার করা হয় যখন আমাদের কিছু **fixed set of values** দরকার হয়, যা বারবার পরিবর্তন হবে না।

### 🔹 Enum কেন দরকার?

Backend application-এ আমরা প্রায়ই role, status, permission ইত্যাদি নিয়ে কাজ করি।
এগুলো যদি string বা number হিসেবে সরাসরি লিখি, তাহলে ভুল হওয়ার সম্ভাবনা থাকে।

Enum এই সমস্যার সমাধান করে।

---

### 🔹 Numeric Enum

* Defaultভাবে enum-এর value সংখ্যা দিয়ে শুরু হয়
* Lightweight এবং fast

👉 সাধারণত internal logic-এর জন্য ব্যবহার করা হয়।

---

### 🔹 String Enum

* Explicit string value থাকে
* Code readable হয়
* Debug করা সহজ

👉 Production backend-এ string enum বেশি জনপ্রিয়।

---

### 🔑 Enum ব্যবহারের সুবিধা

* Magic string/number এড়ানো যায়
* Code consistency বজায় থাকে
* Error কম হয়

---

## 5️⃣ Union and Intersection Types in TypeScript

TypeScript-এর অন্যতম শক্তিশালী feature হলো **type composition**।

---

### 🔹 Union Type (`|`)

Union type বোঝায়—একাধিক type-এর **যেকোনো একটিই valid**।

**ব্যবহারিক ধারণা:**

* User input multiple format হতে পারে
* ID string বা number হতে পারে

👉 Flexible data handle করার জন্য union খুব গুরুত্বপূর্ণ।

---

### 🔹 Intersection Type (`&`)

Intersection type বোঝায়—একাধিক type একসাথে combine হয়ে **একটি নতুন type** তৈরি করে।

**ব্যবহারিক ধারণা:**

* User + Admin
* Person + Employee

👉 Complex object structure তৈরির জন্য intersection ব্যবহার করা হয়।

---

### 🔑 পার্থক্য

* Union → OR logic
* Intersection → AND logic

---

## 🏁 Conclusion

TypeScript-এর এই core concepts গুলো ভালোভাবে বোঝা গেলে:

* Backend code হবে বেশি secure
* Bugs কমবে
* Large-scale application maintain করা সহজ হবে

Node.js Backend Developer হিসেবে এগুলো জানা **অত্যন্ত গুরুত্বপূর্ণ**।

---
